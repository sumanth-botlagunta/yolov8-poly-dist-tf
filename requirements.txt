# Core — TF 2.16 with CUDA 12.5 / cuDNN 9.1
tensorflow==2.16.1
tensorflow-datasets>=4.9.0
# tfds pulls tensorflow-metadata; >=1.16 requires protobuf >=5.26, which conflicts
# with TF 2.16's protobuf 4.25 and breaks `import tensorflow_datasets` at module
# load. Pin below 1.16 for fresh installs on this TF version.
tensorflow-metadata<1.16

# Image augmentation
albumentations>=1.3.0
# headless build: no libGL system dep required (works on CI/servers); same cv2 API
# minus GUI windows, which this codebase never uses. Also what albumentations depends on.
opencv-python-headless>=4.9.0

# Evaluation
pycocotools>=2.0.7
scikit-image>=0.22.0

# Config loading
dacite>=1.8.0

# Utilities
PyYAML>=6.0
absl-py>=2.0.0
numpy>=1.26.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
